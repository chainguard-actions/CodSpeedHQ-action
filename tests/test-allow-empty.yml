name: Test CodSpeed action with allow-empty
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run CodSpeed action with allow-empty
        id: action
        uses: .
        with:
          mode: walltime
          allow-empty: "true"
          skip-hash-check-warning: "false"
        continue-on-error: true

      - name: Assert action completed (success or graceful failure)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "success" ]; then
            echo "PASS: action succeeded with allow-empty=true"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully (network/upload issue expected in harness)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
